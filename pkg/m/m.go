package m

const (
	// Authorization is the metadata keyword for authorization in gRPC.
	Authorization = "authorization"

	// TraceID is the metadata keyword for traceid in gRPC.
	TraceID = "traceid"
)
